import React from "react";

type Props = {};

const Modal = (props: Props) => (
  <article>
    <header></header>
    <section></section>
    <footer>
      <button>Yes</button>
      <button>No</button>
    </footer>
  </article>
);

export default Modal;
